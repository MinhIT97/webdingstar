import styles from '../../../styles/Home.module.scss';
import { HeadNavBar, MobileNavBar } from '../navBar';

function Header() {
  return (
    <div>
      <HeadNavBar/>
      <MobileNavBar/>

    </div>
  );
}

export default Header
